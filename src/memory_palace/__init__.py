"""memory_palace — deterministic single-worker extraction package (v0.1).

Contains the frozen production modules for codex-capture preparation,
extraction, window commit, job queue, and single-worker orchestration.
This package performs no side effects on import.
"""
